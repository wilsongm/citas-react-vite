
const Error = ({children}) => {
  return (
        <div className=" bg-red-700 text-white text-center font-bold mb-3 py-2 rounded-md ">
           {children}
        </div>
  )
}

export default Error
